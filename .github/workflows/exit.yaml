---
name: exit test

on:
  push:

jobs:
  mytest:
    runs-on: ubuntu-latest
    
    steps:
      - continue-on-error:
      - name: test
        run: exit 0
      - name: follow-up
        run: exit 1
      - name: final
        run: echo "HERE!"
